# DebugAssertExperiment

This is a simple experiment to analyze the use of debug assertions in a testing context. The goal is to explore how `Debug.Assert()` behaves in different operating systems and architectures.